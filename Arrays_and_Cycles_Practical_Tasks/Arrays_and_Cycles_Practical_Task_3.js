//Given an array of the names of your friends, add the words hello to each element of the array

let friendsList = ["Alice", "Bob", "Frank"];

let modifiedList = friendsList.map(name => "hello " + name);

console.log(modifiedList)


